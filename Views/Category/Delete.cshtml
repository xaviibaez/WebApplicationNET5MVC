@* Lo que recibimos del controller *@
@model WebApplicationNET.Models.Category
@*Usamos form con metodo post y acción delete - 
    asp-action DeletePost ya que hemos cambiado el nobmre al metodo en el controller y no hace match con la view*@
<form method="post" asp-action="DeletePost">
    @*Por si en algun momento necesitamos enviar mas de un objeto a CategoryController EDIT POST -
        Enviamos escondido el ID*@
    <input asp-for="ID" hidden />
    <div class="border p-3">
        @*Validacion de la view - Esto estaba comentado*@
        <div class="form-group row">
            <h2 class="text-info pl-3">Delete Category</h2>
        </div>
        <div class="row">
            <div class="col-8">
                <div class="form-group row">
                    <div class="col-4">
                        <label asp-for="Name"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="Name" disabled class="form-control" />
                    </div>

                </div>
                <div class="form-group row">
                    <div class="col-4">
                        @*Esto va a recoger el displayName anotation que hay en el model*@
                        <label asp-for="DisplayOrder"></label>
                    </div>
                    <div class="col-8">
                        <input asp-for="DisplayOrder" disabled class="form-control" />
                    </div>

                </div>
                <div class="form-group row">
                    <div class="col-8 offset-4 row">
                        <div class="col">
                            <input type="submit" class="btn btn-danger w-100" value="Delete" />
                        </div>
                        <div class="col">
                            @*Redirection a Index*@
                            <a asp-action="Index" class="btn btn-success w-100"><i class="fas fa-sign-out-alt"></i> Back</a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-4">
                @*Keep this empty*@
            </div>
        </div>
    </div>

</form>

@*Autovalidació del lado del cliente con JS - Cambia el mensaje de error automaticamente - pijeria para mi gusto*@
@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}